import styled from 'styled-components';

const PublicRoomStyle = styled.div`
  width: 20rem;
  height: 20rem;
  background-color: ${props => props.theme.containerColor};
  margin: 10px;
`;

export default PublicRoomStyle;
